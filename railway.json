{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "pip install -r backend/requirements.txt && uvicorn main:app --app-dir backend --host 0.0.0.0 --port $PORT"
  }
}